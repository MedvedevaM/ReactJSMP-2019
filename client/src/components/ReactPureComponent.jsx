import React from 'react';

export default class ReactPureComponent extends React.PureComponent {
    render() {
        return (
            <h2>Hello World with React.PureComponent</h2>
        );
    }
}